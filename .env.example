# Environment variables for n8n and MeTube services.
# Rename this file to .env and fill in your specific values.

# -- General Settings --
# Set your local timezone, e.g., "America/New_York" or "Europe/Berlin"
TZ=America/New_York

# -- n8n Service Settings --
# The domain name n8n will be accessible from.
# This MUST match the domain in your SSL certificate.
N8N_HOST=your-domain.com
# The full webhook URL for n8n.
WEBHOOK_URL=https://your-domain.com:8443/

# -- MeTube Service Settings --
# User and Group ID for file permissions on the host.
# On Linux/macOS, use 'id -u' and 'id -g' in your terminal to find your values.
METUBE_UID=1000
METUBE_GID=1000